pip install wheel

pip install jupyterlab
pip install jupyter

-> jupyter notebook
-> notebook.bat에 위 명령을 넣고 notebook을 더블 클릭

pip install pyserial

pip install matplotlib

pip install pandas

pip install psycopg2
-> DLL load failed while importing _psycopg이 발생한 경우는 최신 버전을 설치
===
pip install psycopg
pip install "psycopg[binary]"